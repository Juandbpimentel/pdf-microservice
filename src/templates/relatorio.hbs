<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: 'Helvetica', sans-serif; font-size: 12px; color: #333; padding: 40px; }
        .summary-box { 
            background: #f4f4f4; padding: 20px; border-radius: 8px; text-align: right; margin-top: 30px; 
            border-left: 5px solid {{corStatus}}; /* Cor dinâmica */
        }
        .big-total { font-size: 28px; font-weight: bold; color: {{corStatus}}; }
        
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th { background-color: #0056b3; color: white; text-align: left; padding: 10px; }
        td { padding: 10px; border-bottom: 1px solid #ddd; }
        tr:nth-child(even) { background-color: #f9f9f9; }
    </style>
</head>
<body>
    {{> header_corporativo }}

    {{> info_grid tituloSecao="Dados do Cliente" itens=dadosCliente }}

    <h3>Extrato de Lançamentos</h3>
    <table>
        <thead>
            <tr>
                <th>Data</th>
                <th>Descrição</th>
                <th style="text-align: right;">Valor (R$)</th>
            </tr>
        </thead>
        <tbody>
            {{#each lancamentos}}
            <tr>
                <td>{{data}}</td>
                <td>{{descricao}}</td>
                <td style="text-align: right;">{{valor}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <div class="summary-box">
        <p>Saldo Final</p>
        <div class="big-total">{{valorTotal}}</div>
        <small>{{statusTexto}}</small>
    </div>
</body>
</html>